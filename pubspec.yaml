name: marketplace_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^4.2.0
  firebase_auth: ^6.1.1
  cloud_firestore: ^6.0.3
  flutter_riverpod: ^3.0.3
  google_fonts: ^6.2.1   # Inter via Google Fonts
  provider: ^6.1.5+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  device_preview: ^1.1.0
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Include folders (semua file di dalamnya otomatis terdaftar)
  assets:
    - assets/icon/
    - assets/image/

    # (Opsional) Daftar eksplisit file yang saat ini ada di proyekmu
    # — aman dibiarkan; tidak masalah jika folder di atas sudah di-include.
    # ICONS
    - assets/icon/AddCartTopRight.png
    - assets/icon/AddProduct2.png
    - assets/icon/AddProduct.png
    - assets/icon/AddProduct3.png
    - assets/icon/CartMidNavbar.png
    - assets/icon/Heart.png
    - assets/icon/Home.png
    - assets/icon/SearchIcon.png
    # - assets/icon/Location.png
    - assets/icon/Notification.png
    - assets/icon/Profile.png

    # IMAGES
    - assets/image/stussy.png
    - assets/image/shoes.png
    - assets/image/nike.png
    - assets/image/puma.jpg
